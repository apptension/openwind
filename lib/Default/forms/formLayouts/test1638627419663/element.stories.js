import Element from './';

export default {
  title: 'Default/forms/formLayouts/test1638627419663',
  component: Element,
};

const Template = (args) => <Element {...args} />;

export const Primary = Template.bind({});
