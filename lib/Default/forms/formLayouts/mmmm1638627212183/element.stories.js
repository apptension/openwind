import Element from './';

export default {
  title: 'Default/forms/formLayouts/mmmm1638627212183',
  component: Element,
};

const Template = (args) => <Element {...args} />;

export const Primary = Template.bind({});
